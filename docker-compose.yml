version: "3.9"

services:
  hub-api:
    build: .
    container_name: hub-api
    restart: always
    env_file:
      - .env
    ports:
      - "80:8080"
    extra_hosts:
      - "host.docker.internal:host-gateway" 
